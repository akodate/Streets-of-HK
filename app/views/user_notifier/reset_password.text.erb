We received a request to reset your Streets of HK credentials. To do so, please follow the coded link below:

<%= reset_url(@user.code) %>

If you received this email in error, simply do nothing. The above link will expire at
<%= @user.expires_at.strftime("%-I:%M on %a, %-d %b %Y") %>

Thanks!
Streets of HK